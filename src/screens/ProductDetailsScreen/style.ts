import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        marginTop: 25,
    },cartContainer:{
        alignContent: 'center',
        margin: 30,
        alignSelf: 'flex-end',
    },
    cartImage:{
        
    },
    productContainer:{
        alignContent: 'center', 
    }
});

export default styles;